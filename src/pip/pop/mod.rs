#[test]
fn test_pop() {
    assert_eq!(1, 1);
}

mod shes {
    mod a {
        #[cfg(test)]
        mod whore {
            #[test]
            fn test_pop() {
                assert_eq!(1, 1);
            }

            #[test]
            fn test_push() {
                assert_eq!(2, 2);
            }
        }
    }
}